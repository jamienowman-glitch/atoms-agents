## Group view

- Stack multiple accordion items with `accordion_item_gap` token and optional `accordion_divider_stroke` between them.
- Demonstrate single-open mode: expanding one item collapses others; multi-open shows multiple expanded bodies.
- Show desktop vs mobile spacing adjustments; headers remain full-width within the 24-column container.
- Focus order follows document order; chevron rotates per item state without desync.

