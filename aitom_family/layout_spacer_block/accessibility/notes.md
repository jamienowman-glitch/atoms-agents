## Accessibility notes

- Set `aria-hidden="true"` on spacer wrappers and omit roles; if framework requires a role, use `presentation`.
- Keep spacer elements out of the tab order; no focus ring or interactive affordance should appear.
- Ensure any debug outlines or labels stay aria-hidden so assistive tech ignores layout-only spacing.

