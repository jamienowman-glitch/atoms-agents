## Icons

- Spacer uses [NO ICONS] by default; there is no interactive handle.
- If debug handles are enabled, reference a simple grip/drag icon token from the shared registry and size/colour it via `spacer_debug_handle_size` and `spacer_debug_handle_colour` tokens instead of hardcoded values.

