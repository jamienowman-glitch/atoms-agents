## Typography

- Spacer has no text content; mark as [NO TYPOGRAPHY].
- If debug labels are surfaced for tooling, reuse the shared meta/label preset from theme_typography_settings (e.g., `tokens.typography.meta`) and keep labels aria-hidden.

