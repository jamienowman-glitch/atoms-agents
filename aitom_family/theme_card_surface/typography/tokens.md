## Typography hooks

- Card-level badges/labels may use:
  - `card_label` preset for small flags (e.g., “NEW”, “SALE”).
  - `card_meta` preset for auxiliary text on the shell (not main content).
- Presets reference theme typography settings; no unique family required.
