## Typography settings layout

- If surfaced, use 24-column grid: default span 24/24 mobile, 12/24 tablet, 8/24 desktop.
- List presets in a vertical stack with `preset_gap` token; optional two-column layout on desktop.
- Include preview text aligned left; keep padding consistent with theme_layout_settings `section_padding`.
- If no UI is shown, mark [NO LAYOUT] and rely on configuration-only usage.
