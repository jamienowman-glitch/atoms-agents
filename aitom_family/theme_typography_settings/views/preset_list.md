## Preset list view

- Display preset samples (h1–h6, body, label, button, caption) on black background with white text.
- Mobile: single column stack; Desktop: two-column grid within 24-column span.
- Each row shows preset name + sample line; selection indicated via stroke and focus ring tokens.
- Include density toggle preview (compact vs comfortable line-height).
