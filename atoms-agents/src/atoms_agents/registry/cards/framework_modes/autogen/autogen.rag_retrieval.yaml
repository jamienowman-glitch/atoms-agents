card_type: mode
id: autogen.rag_retrieval
framework: autogen
official_name: RAG Retrieval
invoke_primitive: AssistantAgent(name, model_client, memory)
entrypoint: northstar.runtime.modes.autogen.autogen_rag_retrieval.run
params:
  official_name: Memory (RAG) via AssistantAgent
  official_options: model_client, memory list
  minimal_snippet: assistant = AssistantAgent(name="assistant", model_client=client,
    memory=[ChromaDBVectorMemory(collection_name="docs")])
streaming_support: true
docs_urls:
- https://microsoft.github.io/autogen/agentchat/memory#rag-retrieval
pinned_version: AutoGen v0.4.0
confidence: high
notes: AutoGen uses AssistantAgent with memory stores (e.g. ChromaDBVectorMemory)
  for RAG
