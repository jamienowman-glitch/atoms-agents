name: Demo Shop Connector
provider_slug: shopify_demo
secrets:
  - key: access_token
    label: Admin Access Token
  - key: api_secret
    label: API Secret Key
scopes:
  - read_products
  - write_orders
requires_firearm: true
