# God Mode Domain Config
# This is the SOURCE OF TRUTH for the System Domain.
# When this changes, Agents use tools to update Supabase and Resend.

system:
  domain: "squared-agents.app"
  production: true
  
  # ------------------------------------------------------------------
  # FUTURE MIGRATION NOTE:
  # When you eventually buy 'atoms-fam.app', follow these steps:
  # 1. Update 'domain' above to "atoms-fam.app".
  # 2. Update 'sender_email' below to "aissistant@atoms-fam.app".
  # 3. Agents will detect the change and prompt you to:
  #    a. Add the new domain to Resend.
  #    b. Update Supabase SMTP settings.
  #    c. Update Supabase URL Configuration.
  # ------------------------------------------------------------------

email:
  # Confirmed Verified on 2026-01-26
  sender_email: "aissistant@squared-agents.app"
  provider: "resend"

  # SMTP SETTINGS FOR SUPABASE (Enter these in Supabase Dashboard):
  # Host: smtp.resend.com
  # Port: 465
  # User: resend
  # Pass: [Get from Vault: resend-key.txt]
