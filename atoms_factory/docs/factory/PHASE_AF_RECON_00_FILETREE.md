# Atoms Factory File Tree

## Structure Analysis

*   **Atoms Location:** `atoms/aitom_family`
*   **Bucket Compliance:** Partial.
    *   **Present:** `views`, `data_schema`, `tracking`, `accessibility`, `exposed_tokens`, `behaviour`, `icons`.
    *   **Mixed/Nested:** `layout` and `colors` often found inside `exposed_tokens` rather than top-level. `typography` found in both.

## Directories
*   `atoms/aitom_family`: Contains all atom definitions.
*   `fonts`: Token-driven font presets.
*   `apps`: Example apps (workbench).
