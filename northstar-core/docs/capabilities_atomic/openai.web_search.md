# OpenAI Web Search
**ID:** `openai.web_search`

## Usage
Tools-based web search using the `web_search` tool type.

## Parameters
- `count` (int): Number of results to return (default: 5).

## Test Plan
- **Type:** Live Once (Cheap)
- **Status:** Verified structure via Atomic Runner.
