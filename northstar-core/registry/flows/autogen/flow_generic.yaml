name: generic_autogen_flow
id: registry/flows/autogen/flow_generic.yaml
description: "Generic container for multi-agent debate."
runtime: autogen

config:
  type: group_chat
  max_round: 6
  speaker_selection_method: round_robin
  admin_name: "admin"

participants: [] # To be injected at runtime

steps:
  - id: round_1
    participant_id: PLACEHOLDER
  - id: round_2
    participant_id: PLACEHOLDER
