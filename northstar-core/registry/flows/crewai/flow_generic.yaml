name: generic_crewai_flow
id: registry/flows/crewai/flow_generic.yaml
description: "Generic container for a single agent task."
runtime: crewai

steps:
  - id: generic_step
    agent_id: PLACEHOLDER_AGENT
    template_id: generic_task
    inputs:
      - input_text
    expected_output: "Task completion."
