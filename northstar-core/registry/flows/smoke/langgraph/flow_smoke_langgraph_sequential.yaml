id: flow_smoke_langgraph_sequential
runtime: langgraph
description: Smoke test for LangGraph Sequential Mode
mode: langgraph:sequential_chain
steps:
  - id: step_1
    template_id: default_step
    inputs:
       input_text: "Blackboard Input"
blackboards:
  - global
