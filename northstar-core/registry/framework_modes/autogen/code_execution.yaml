description: Demonstrates AutoGen's ability to write and execute code (Local/Docker).
framework: autogen
parameters:
  code_task:
    type: string
    description: Task requiring code execution.
    default: Print hello world to python
mode_id: AutoGen.CodeExecution
invoke_entrypoint: runtime.autogen.modes.code_execution.run
supports_streaming: none
required_params:
- code_task
pinned_version_ref: integration/autogen/PINNED_VERSION.md
notes: UNKNOWN
