framework: autogen
mode_id: AutoGen.HumanInput.Strategy
description: |
  Granular control over when to solicit human input.
parameters:
  human_input_mode:
    type: string
    enum: ["ALWAYS", "TERMINATE", "NEVER"]
    default: "NEVER"
entrypoint: null
supports_streaming: token
