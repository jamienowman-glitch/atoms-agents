framework: autogen
mode_id: AutoGen.Selection.Random
description: |
  Speaker selection strategy that randomly picks the next speaker.
  Useful for brainstorming or breaking deterministic loops.
parameters:
  seed:
    type: integer
    description: Optional seed for reproducibility.
entrypoint: null
supports_streaming: token
