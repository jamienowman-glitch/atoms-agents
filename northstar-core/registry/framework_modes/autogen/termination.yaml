framework: autogen
mode_id: AutoGen.Termination
description: |
  Advanced termination strategies for agent conversations.
parameters:
  strategy:
    type: string
    enum: ["max_turns", "stop_token", "consensus"]
  max_turns:
    type: integer
    default: 10
  stop_token:
    type: string
    default: "TERMINATE"
entrypoint: null
supports_streaming: token
