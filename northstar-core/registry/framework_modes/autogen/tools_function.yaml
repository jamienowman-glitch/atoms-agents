description: Demonstrates AutoGen's function calling capabilities.
framework: autogen
parameters:
  input_text:
    type: string
    description: Input requiring tool usage.
    default: What is length of 'hello'?
mode_id: AutoGen.Tools.Function
invoke_entrypoint: runtime.autogen.modes.tools_function.run
supports_streaming: none
required_params:
- input_text
pinned_version_ref: integration/autogen/PINNED_VERSION.md
notes: UNKNOWN
