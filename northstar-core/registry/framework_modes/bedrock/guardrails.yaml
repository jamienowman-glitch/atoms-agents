description: Applies Bedrock Guardrails to filter or flag unsafe content.
framework: bedrock
parameters:
  input_text:
    type: string
    description: Text to check against guardrails.
    default: This is safe text.
  guardrail_id:
    type: string
    description: ID of the specific guardrail to apply.
    default: MOCK_GUARDRAIL
mode_id: Bedrock.Guardrails
invoke_entrypoint: runtime.bedrock.modes.guardrails.run
supports_streaming: none
required_params:
- input_text
- guardrail_id
pinned_version_ref: integration/bedrock/PINNED_VERSION.md
notes: UNKNOWN
