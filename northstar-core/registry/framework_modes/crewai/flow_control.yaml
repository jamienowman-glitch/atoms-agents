framework: crewai
mode_id: CrewAI.Flow.Control
description: |
  Advanced Flow orchestration patterns including multi-start,
  listeners (event-driven), and explicit routing logic.
parameters:
  pattern:
    type: string
    enum: ["start", "listen", "router"]
    description: The orchestration primitive to apply.
  parallel_execution:
    type: boolean
    default: true
entrypoint: null
supports_streaming: event
