description: Demonstrates creating and assigning a custom Tool to a CrewAI Agent.
framework: crewai
parameters:
  text_input:
    type: string
    description: Text to analyze using the custom tool.
    default: Hello World
mode_id: CrewAI.Tools.Custom
invoke_entrypoint: runtime.crewai.modes.tools_custom.run
supports_streaming: none
required_params:
- text_input
pinned_version_ref: integration/crewai/PINNED_VERSION.md
notes: UNKNOWN
