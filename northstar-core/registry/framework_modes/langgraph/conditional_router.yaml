description: Demonstrates conditional routing (branching) in LangGraph.
framework: langgraph
parameters:
  decision:
    type: string
    description: 'The path to take: ''A'' or ''B''.'
    default: A
mode_id: LangGraph.ConditionalRouter
invoke_entrypoint: runtime.langgraph.modes.conditional_router.run
supports_streaming: none
required_params:
- decision
pinned_version_ref: integration/langgraph/PINNED_VERSION.md
notes: UNKNOWN
