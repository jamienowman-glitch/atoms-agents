description: A chatbot mode with persistent long-term memory using LangGraph Store.
framework: langgraph
parameters:
  user_id:
    type: string
    description: Unique identifier for the user to scope memory.
    default: default_user
  input_text:
    type: string
    description: User input message.
mode_id: LangGraph.Memory
invoke_entrypoint: runtime.langgraph.modes.memory.run
supports_streaming: none
required_params:
- user_id
- input_text
pinned_version_ref: integration/langgraph/PINNED_VERSION.md
notes: UNKNOWN
