description: Executes multiple branches in parallel (DAG pattern).
framework: strands
parameters:
  branches:
    type: array
    items:
      type: string
    description: List of branch names to execute in parallel.
    default:
    - Branch A
    - Branch B
mode_id: Strands.DAG
invoke_entrypoint: runtime.strands.modes.dag_parallel.run
supports_streaming: none
required_params:
- branches
pinned_version_ref: UNKNOWN
notes: UNKNOWN
