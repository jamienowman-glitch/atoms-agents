description: Executes a defined sequence of steps in a deterministic linear order.
framework: strands
parameters:
  steps:
    type: array
    items:
      type: string
    description: List of steps to execute sequentially.
    default:
    - Initialize
    - Process
    - Finalize
mode_id: Strands.Linear
invoke_entrypoint: runtime.strands.modes.linear_sequence.run
supports_streaming: none
required_params:
- steps
pinned_version_ref: UNKNOWN
notes: UNKNOWN
