card_type: mode
id: adk.sessions_memory
framework: adk
official_name: Sessions Memory
invoke_primitive: InMemorySessionService.create_session
entrypoint: northstar.runtime.modes.adk.adk_sessions_memory.run
params:
  official_name: Session (with MemoryService)
  official_options: app_name, user_id, state
  minimal_snippet: service = InMemorySessionService(); session = await service.create_session(app_name="my_app",
    user_id="user", state={"key":"value"})
streaming_support: false
docs_urls:
- https://google.github.io/adk-docs/sessions/session/
pinned_version: ADK Python v0.1.0 (Dec 2025)
confidence: med
notes: "ADK uses SessionService and MemoryService (e.g. InMemorySessionService) ;\
  \ no single \u201Csessions_memory\u201D mode in docs"
