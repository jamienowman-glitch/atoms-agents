card_type: provider
provider_id: openrouter
name: OpenRouter
required_fields:
  - openrouter_api_key
notes: Aggregator for OpenAI, Anthropic, Google, etc.
---
card_type: provider
provider_id: nvidia
name: NVIDIA NIM
required_fields:
  - nvidia_api_key
notes: NVIDIA-hosted Foundation Models.
---
card_type: provider
provider_id: comet
name: Comet
required_fields:
  - comet_api_key
notes: Comet LLM Provider.
---
card_type: provider
provider_id: gemini
name: Google Gemini
required_fields:
  - gemini_api_key
notes: Multimodal models (Video, Image).
